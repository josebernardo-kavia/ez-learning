# Default to dev profile for local/preview runs
spring.profiles.active=dev

spring.application.name=ez-learning

# Flyway scans classpath:db/migration by default.
# Create SQL migrations like V1__init.sql under src/main/resources/db/migration for schema evolution.
